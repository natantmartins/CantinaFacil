<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">
            @drawable/backgroud_splash
        </item>
        <item name="android:statusBarColor">
            @color/primary
        </item>
        <!-- Customize your theme here. -->
    </style>

</resources>
